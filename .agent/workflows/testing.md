---
description: 
---

# Testing Workflow

// turbo-all

To test the application efficiently, follow these steps:

1. **Run Automated Tests**:
   - Prompt the user to run `pnpm prepare` and then `pnpm test` to re-run the tests.